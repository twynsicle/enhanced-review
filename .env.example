# enhanced-review — environment template.
#
# Copy to `.env` (gitignored) and fill in real values. Every key is validated
# by src/config/env.ts at startup; an invalid or missing required value fails
# the boot with a message naming the key.

# --- Server ------------------------------------------------------------------

# development | production | test. `npm run dev` leaves it unset (development);
# `npm start` and the container set production.
# NODE_ENV=development

# HTTP port for the Express server.
# PORT=3000

# Reported by GET /api/health. The container build sets it to the image tag.
# APP_VERSION=

# --- Logging -----------------------------------------------------------------

# Pino level: trace | debug | info | warn | error | fatal | silent.
# LOG_LEVEL=info

# 1 = human-friendly coloured output via pino-pretty (dev only).
# LOG_PRETTY=1

# --- Database ----------------------------------------------------------------

# Postgres connection string (Prisma + pg). The value below matches
# docker-compose.yml's postgres service.
DATABASE_URL=postgresql://enhanced_review:enhanced_review@127.0.0.1:5432/enhanced_review

# --- Auth --------------------------------------------------------------------

# Signs the er_session and gh_access_token cookies. At least 32 characters;
# rotating it signs everyone out. Generate one with:
#   node -e "console.log(require('node:crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=

# GitHub OAuth App (Settings > Developer settings > OAuth Apps). Its
# authorization callback URL must be <APP_ORIGIN>/auth/github/callback.
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Public origin the app is served from; builds the OAuth redirect URI and
# turns on Secure cookies when https.
APP_ORIGIN=http://localhost:3000

# --- Review runner -----------------------------------------------------------

# stub | claude. `stub` streams a canned review with no API calls (the local
# default); `claude` runs the Claude Agent SDK against the cloned repository.
REVIEW_EXECUTOR=stub

# Claude model identifier. Ignored when REVIEW_EXECUTOR=stub.
# REVIEW_MODEL=claude-haiku-4-5

# Forwarded to the SDK subprocess when REVIEW_EXECUTOR=claude
# (CLAUDE_CODE_OAUTH_TOKEN is honoured too).
# ANTHROPIC_API_KEY=

# Minutes a review may run before it is marked `error` and aborted.
# REVIEW_TIMEOUT_MIN=15

# Pending-or-running reviews one user may have at once; the next submit is
# refused with a link to the in-flight job.
# MAX_JOBS_PER_USER=1

# --- Scheduler ---------------------------------------------------------------

# 0 turns the scheduler loop off without touching the schedules themselves:
# they stay in the database and in the UI, nothing fires. Every process that
# serves traffic also runs the loop, so a second instance would double-fire —
# see docs/OPERATIONS.md#scheduled-reviews.
# SCHEDULER_ENABLED=1

# How often the loop looks for due schedules, and how many it may claim per
# tick. Claiming is a conditional `active -> running` update, so a tick that
# overruns the interval cannot take the same schedule twice.
# SCHEDULER_TICK_MS=60000
# SCHEDULER_BATCH_SIZE=5

# A GitHub personal access token (repo scope) that scheduled runs clone with.
# A user's own token lives only in their cookie and is never persisted, so
# without this the loop stays idle and says so in the log once per boot.
# SCHEDULER_GITHUB_TOKEN=

# Consecutive failed launches before a schedule is parked as `failed`, and how
# long it waits between attempts. The owner resumes it from /schedules.
# SCHEDULE_MAX_FAILURES=3
# SCHEDULE_RETRY_BACKOFF_MIN=10

# Schedules one user may have at once, across every repository.
# MAX_SCHEDULES_PER_USER=10

# --- Polling (browser) -------------------------------------------------------

# How often the live job view asks for new chunks, and how often the
# cross-page notifier checks for finished reviews. Both pause on hidden tabs.
# LIVE_POLL_MS=2000
# TERMINAL_POLL_MS=10000
