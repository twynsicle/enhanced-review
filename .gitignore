# dependencies
/node_modules

# build output (react-router build, jobs bundle) and generated route types
/build
/.react-router

# tests
/coverage

# env files — .env.example is the committed template
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
*.tsbuildinfo
npm-debug.log*

# PocketBase-era local state. Kept on disk until the Monaco diff baseline is
# captured against `main` (docs/rr-migration/phase-1-plan.md, P1-D7); these
# two lines go in Phase 6.
/tools/pocketbase/
/pb_data/

# claude code (user-local permission overrides; project-shared settings.json is fine)
.claude/settings.local.json

# playwright mcp runtime artifacts (console logs + page snapshots)
.playwright-mcp/

# local UI baseline + phase screenshots for the RR migration
/screenshots/
